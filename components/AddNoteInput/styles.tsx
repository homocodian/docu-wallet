import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleInput: {
    fontSize: 22,
    fontWeight: "600",
  },
  noteInput: {
    fontSize: 20,
    fontWeight: "600",
    paddingTop: 15,
  },
});
